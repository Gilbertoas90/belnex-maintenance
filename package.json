{
  "name": "belnex-energy-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:images && vite",
    "build:images": "node scripts/optimize-images.mjs",
    "build": "npm run build:images && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.33.5",
    "vite": "^5.4.21"
  }
}
